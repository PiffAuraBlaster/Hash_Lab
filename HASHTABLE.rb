require 'bigdecimal'

class MyHash
  def initialize(p, w, A)
  end
  
  def insert(key, val)
    #Inserts a new hash value into the table and assigns it a key.
  end
  
  def delete(key)
    #Deletes a hash when given the key.
  end
  
  def search(key)
    #Finds a hash and value in hash table when given a key.
  end
  
end

